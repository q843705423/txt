对于定时执行的问题，在现有的代码中,每当有一个客户端连接时，就会开启一个定时器，那么这样原来每10秒执行1次存储过程就会变成每10秒执行n次，n取决于客户端浏览器的数量。
这显然是不被接受的，因此。
我们不能以客户端请求作为其中的直接标准
，而是以一个后台做一个定时器，
然后同步到后台的一个变量中，最好使用Hash表加大效率
这样无论多少客户端连接，每10秒只能有1次与数据库的连接，调用存储过程，这才是正确的思路。
因此还是需要客户端的代码。
